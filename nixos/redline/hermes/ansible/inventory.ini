; Run from redline:
;   cd nixos/redline/hermes/ansible && ansible-playbook -i inventory.ini hermes.yml
;
; The VM is reachable only over the host-only bridge, so this inventory is
; unusable from anywhere else. That is the intent.
[hermes]
10.100.0.2

[hermes:vars]
ansible_user=philpax
ansible_python_interpreter=/usr/bin/python3

# Managed by ansible. Edit ../hermes.yml, not this file.
[Unit]
Description=Back up HERMES_HOME to the host drop directory
After=srv-drop.mount
RequiresMountsFor=/srv/drop

[Service]
Type=oneshot
ExecStart=/usr/local/bin/hermes-backup
User=root
